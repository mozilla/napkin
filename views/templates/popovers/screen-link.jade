script#screen-link-popover-template(type='text/ejs')
  form
    .field
      label(for='text') Link text:
      input#text(name='text', type='text', value!='<%= text %>')

    .field
      label(for='screen') Link to screen:
      != partial('templates/helpers/screen-selection', { selected: 'this.screen', name: 'screen' })

    != partial('templates/popovers/footer')
