script#auth-popover-template(type='text/ejs')
  form
    .field
      label(for='redirect') On login, redirect to screen:
      != partial('templates/helpers/screen-selection', { selected: 'this.redirect', name: 'redirect' })

    != partial('templates/popovers/footer')
